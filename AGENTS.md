# AGENTS.md

Canonical instructions for AI coding agents working in this repository.

## Project

- Name: ccc-frontend (CCC on-demand UI)
- Purpose: Operator web UI for the CCC tailor API — paste a JD, download a tailored `.docx`, read recruiter `replyText`. Next milestone: Kanban job tracker. Gmail inbound drafts live in the companion [CCC](../CCC) repo, not here.
- Companion: `~/coding/CCC` — API, inbox worker, Master CV, product STRATEGY thesis. Prefer linking/copying contracts from there over inventing API behavior.
- Primary stack: Node.js 22, TypeScript 5, Next.js 15 (App Router), React 19, Tailwind 4, npm.

## Working Rules

- Read existing code and docs before changing behavior.
- Keep changes scoped to the user's request.
- Preserve user edits and unrelated dirty worktree changes.
- Prefer existing local patterns over introducing new abstractions.
- Update docs when behavior, architecture, or test strategy changes.
- Follow TDD for new behavior and bug fixes (see Testing Philosophy).
- For complex multi-step tasks (planning, decomposition, review), reason through fully before producing any artifact. Do not narrate the reasoning process in output — lead with conclusions and artifacts.
- Never put `TAILOR_API_KEY` (or any Bearer secret) in client bundles, `NEXT_PUBLIC_*` vars, or browser-visible responses.

# Git Branch Safety

You operate under a locked-down branch policy. The `main` branch is protected at the remote level (PR-only). Your local behavior must align.

## Hard Constraints (Do Not Violate)

- **Never** write files, edit code, or run any git write operation (`commit`, `push`, `merge`, `rebase`) while checked out on `main`.
- **Never** proceed without verifying the active branch when a task involves file writes or git commands.
- If you are on `main` and the task requires code changes, **stop and create a feature branch first**. Do not ask — just do it.

## Required Workflow

```
git branch --show-current → on main?
  → YES: git checkout -b feature/<descriptive-slug>
  → NO:  proceed on current branch
```

All implementation, testing, and review work happens on feature branches exclusively.

## Git Operations Checklist

Before running `git commit`, `git push`, `git merge`, or `git rebase`:
1. Run `git branch --show-current`
2. If output is `main` → abort the operation. Create a feature branch first.

## Push and PR

- Push only feature branches (`git push -u origin HEAD`).
- After pushing, tell the user: "Branch pushed. Open or refresh the PR and wait for CodeRabbit before merging."
- **Never** push `main` directly.

## Wrong / Right

**Wrong:** Writing implementation files directly on `main` and then committing.
**Right:** `git checkout -b feature/my-task` → write files → commit → push → "Open a PR when ready."

**Wrong:** Assume the branch is correct without checking.
**Right:** Run `git branch --show-current` before any git operation or code modification.

# Communication

- Be extremely concise
- Prefer bullets over paragraphs
- Never explain what you're about to do
- Never summarize what you just did
- Maximum 3 prose sentences unless explicitly necessary

**Wrong:** "I'll now read the plan file to understand the task scope..."
**Right:** Read the file. Lead with what you found.

**Wrong:** "I've completed the implementation. Here's a summary of what I did..."
**Right:** Emit the artifact or the run command. Stop.

# Scope

- Implement ONLY the assigned task
- Do not silently redefine architecture
- Do not silently refactor unrelated areas
- Keep changes tightly bounded to the task
- Preserve existing conventions unless asked to change them
- Do not rebuild Gmail / inbox scan here — that stays in CCC

# Uncertainty

**Internal ambiguity** — stop and escalate. If you encounter any of the following, do not guess and do not continue:
- architectural ambiguity
- conflicting constraints
- security or auth uncertainty
- billing or payment logic
- data model ambiguity

Document findings in the active plan file under `docs/plans/` or create a solution entry in `docs/solutions/` and halt. Never write placeholder entries (e.g. "None", "N/A", "No blockers") — if there is nothing to report, leave the section empty.

**External ambiguity** — search before stopping. If uncertainty concerns the behavior of a third-party library, external API, or public spec (e.g., "what does CCC return on a 429?"), use web search and/or the companion CCC docs first. Stop and escalate only if the question remains unresolved after searching.

# Context Discipline

**Plan files live in `docs/plans/`** as date-prefixed kebab-case markdown files (`YYYY-MM-DD-<type>-<name>-plan.md`), generated by `/ce-plan`. Institutional learnings live in `docs/solutions/`. Product thesis for CCC overall lives in companion `STRATEGY.md`; this repo’s `STRATEGY.md` covers frontend scope only.

**Before any non-trivial work (planning, implementation, or review):** read, in order, only what you need — do not load the whole tree by default.

**Stale plan check:** plan filenames include the creation date. If the most recent plan in `docs/plans/` is more than 14 days old, treat that as a staleness *signal* and ask the user — but when `docs/plans/README.md` exists, its **Active milestone** (and linked plan) is authoritative for what to work on, even if a newer-dated plan file exists. If Active says no in-progress plan, halt — do not invent work — **except** when the operator has started **Drain** (below). Nested `/lfg` or `/ce-work` with an explicit plan path or drain target must not take this Active-empty halt.

**Work queues:** Trust `docs/plans/README.md` for what to build next (lanes + Active). STRATEGY “Not working on” is a deny-list.

**Drain** (operator-started `/lfg` iterator; not a daemon):

1. **Kickoff.** Run Drain only when the operator starts it. `/lfg` remains the per-item pipeline; Drain iterates README `lfg` rows.
2. **Composite pick.** Re-read `docs/plans/README.md` and open PRs. Next job is the first `lfg` row in board order that does not already have an open **drain PR**. Skip `operator`, `parked`, `awaiting-merge`.
3. **Join key.** A drain PR names its row in the body as `Drain-row: <id>` and should use branch `lfg/<row-slug>`.
4. **Target.** Invoke **full** `/lfg` with the explicit target from that row.
5. **Babysit-clean, not DONE.** After `/lfg` returns, inspect babysit `{status, residuals}`. Prefer the same agent follows `/lfg` then resumes Drain.
6. **Own-row `awaiting-merge`.** On **that item's PR branch**, never on `main`: set only that row to `awaiting-merge`, commit, push, then babysit once more before the next row.
7. **Next row.** Never write on `main`. New feature branch per `/lfg`.
8. **Halt success.** Empty `lfg` set is success — do not invent work.

| When | Read (in order) | Purpose |
|------|-----------------|---------|
| Session / task start | `docs/plans/README.md` (active milestone), then its linked plan(s) | Objective, constraints, acceptance criteria |
| Before coding against CCC | `docs/api/API.md` (mirror; SoT may be companion CCC) | Tailor request/response contract |
| When orienting to domain terms | `CONCEPTS.md` | Shared vocabulary |
| When debugging past decisions | `docs/solutions/` | Prior learnings and gotchas |
| Product thesis / deny-list | `STRATEGY.md` (this repo) + companion CCC `STRATEGY.md` | Frontend scope vs overall CCC bet |

**Handoffs:** prefer `@docs/plans/...`, `@docs/api/...`, or `@docs/solutions/...` plus **scoped** git context over long chat transcripts.

- Prefer artifact references over long chat history.
- Codebase reality overrides conversation history. If artifacts contradict stale discussion, follow the artifacts.

## Commands

```bash
# Install dependencies
npm install

# Run development server (default port 3000; run CCC on 3001)
npm run dev

# Run tests (mocked CCC; no live LLM)
npm test

# Lint
npm run lint

# Build
npm run build
```

## Coding Practices

### Frontend + BFF

- **Secrets stay server-side.** `TAILOR_API_KEY` and `CCC_API_URL` are server env only. Never prefix them with `NEXT_PUBLIC_`.
- **This app is a Bearer presenter.** Browser → `/api/tailor` → CCC `POST /api/tailor-cv` with `curationMode: "strict"`. Do not call CCC from the client.
- **Fail closed on missing env.** Missing `CCC_API_URL` / `TAILOR_API_KEY` → client-safe 503; never echo the key.
- **Error strings are client-safe.** Strip anything that looks like secrets before returning messages to the UI.
- **Match existing module style.** Named exports, discriminated unions (`{ ok: true } | { ok: false }`), explicit return types.

### Testing Philosophy

**TDD is the default.** For new behavior and bug fixes: write a failing test first, then the minimum implementation to pass, then refactor.

**Red → Green → Refactor.**
1. **Red** — add or extend a test that expresses the desired behavior; run it and confirm it fails for the right reason.
2. **Green** — implement the minimum to make that test pass; do not expand scope while red.
3. **Refactor** — clean up structure and duplication while keeping the suite green.

**Never ship green without having seen red.**

**Exceptions (document why in the PR/plan when used):** thin route/wiring glue that only composes already-tested units; time-boxed spikes (backfill tests before merge); pure docs/config with no runtime behavior.

**Test behavior, not prose.** Assert inputs, outputs, side effects, error paths — not markdown headings.

**Don't test the framework.** Trust TypeScript for structural contracts.

**One test file per production module** when practical. `ccc-tailor.ts` → `tests/ccc-tailor.test.ts`. Integration/scenario tests may use descriptive names under `tests/`.

### Senior Engineer Heuristics

**80% overlap means extend, don't copy.**
**External data is `unknown` until validated.**
**`.env.example` is the canonical env var catalog.** Every new env var must appear there with a comment.
**Read the target module before touching it.**
**Write the type signature first.**
**Internal critic before declaring done:** secrets in client? hardcoded timeouts that belong in env? near-duplicate functions? `as`-casts on external data?

## Documentation Map

- `docs/api/`: CCC tailor contract mirror (SoT may live in companion CCC until drift process exists).
- `docs/arch/`: frontend architecture, module boundaries, BFF invariants.
- `docs/plans/`: CE plan artifacts. `docs/plans/README.md` is the milestone north-star.
- `docs/solutions/`: institutional learnings (bugs, practices, workflow patterns) with YAML frontmatter where present.
- `docs/brainstorms/`, `docs/ideation/`, `docs/residual-review-findings/`: CE workflow artifacts.
- `docs/archive/`: local historical notes (gitignored by default).
- All directories and markdown filenames under `docs/` use kebab-case (`README.md` exception). Update the nearest `README.md` index when adding/moving docs.

## Agent-Specific Entrypoints

- `CLAUDE.md` imports this file with `@AGENTS.md`.
- `CODEX.md` points users to this file.

Keep long-lived instructions here so agent-specific files do not drift.

# Compound-Engineering Workflows

This project uses the compound-engineering skill suite. Cursor via the compound-engineering plugin; Pi via [`pi-compound-engineering`](https://pi.dev/packages/pi-compound-engineering) when configured.

| Purpose | Command |
|---------|---------|
| Explore ideas before planning | `/ce-brainstorm` |
| Create a structured plan in `docs/plans/` | `/ce-plan` |
| Execute work from the active plan | `/ce-work` |
| Multi-agent code review | `/ce-code-review` |
| Document learnings → `docs/solutions/` | `/ce-compound` |
| CE health / local config | `/ce-setup` |

The core loop is: **Plan → Work → Review → Compound**.

Before committing, run lint and tests:
```bash
npm run lint
npm test
```

# Subagents

In Cursor, use the Task tool / Explore agents with an explicit model override when selecting Composer: **`composer-2.5` only — never `composer-2.5-fast`** (or any other `*-fast` Composer slug), including scouts, grounding, and “cheap extraction” work. Omit `model` to inherit only when the parent session is verified Auto, standard `composer-2.5`, or Grok; if the parent is any `*-fast` Composer slug, pass `composer-2.5` explicitly.

## Model Tier Assignments

### Mid tier — implementation, review, debugging (default)

| Role | Model |
|------|-------|
| **Breadth (review/debugging)** | `deepseek/deepseek-v4-pro` |
| **Precision (review/debugging)** | `openrouter/moonshotai/kimi-latest:max` |
| **Second opinion** | `cursor-grok-4.5-high` or `openrouter/z-ai/glm-5.1:max` |

**Cursor tier mapping:**

| CE Tier | Thinking needed? | Cursor Model | When |
|---|---|---|---|
| Low | No | `agent --model auto` | Simple fixes, formatting, boilerplate |
| Mid | No | `agent --model composer-2.5` | Multi-file, new logic, standard refactoring |
| Mid | Yes | `agent --model cursor-grok-4.5-high` | Debugging, test writing, design decisions |
| High | — | `agent --model cursor-grok-4.5-high` | Architecture, tricky logic, cross-cutting concerns |

**Never use Fast Composer** for subagents, Explore, scouts, or parent-session coding in this repo.

Full cost-quality data: `docs/solutions/mid-tier-review-models-match-frontier-at-7x-cost.md`

# Review Philosophy

- Optimize for correctness over cleverness
- Flag uncertainty explicitly
- Provide evidence for claims
- Confidence must match actual certainty — do not overstate

### Confidence Level Definitions

- **High:** Code path is deterministic, issue is reproducible, no credible alternative interpretation.
- **Medium:** Issue is likely but an alternative explanation exists, or the traced path has gaps.
- **Low:** Issue is possible but requires assumptions about runtime behavior or external state not visible in the diff.

## Learned User Preferences

- When fixing review findings: verify each against current code; fix only still-valid issues; skip the rest with a brief reason; keep changes minimal and validate.
- Prefer asking when product/content ambiguity remains rather than guessing.
- Do not put `TAILOR_API_KEY` in the browser.
- Gmail / inbox stays in CCC; this repo is on-demand UI + future job tracking.
- Prefer operator review of CV quality over on-path LLM judges.
- When asked to ship fixes to an already-open PR, verify the active branch first; never write/commit/push on `main`.
- Calibrate model choice per task; never select Fast Composer for Task/Explore/scouts.

## Learned Workspace Facts

Pointer-first bootstrap:

- This app defaults to port **3000**; run CCC on **3001** (or set `CCC_API_URL`).
- Server route `app/api/tailor` presents Bearer to CCC; client talks only to this app.
- Strict on-demand path: `curationMode: "strict"` → `.docx` (`cv`) + optional `replyText`.
- Unit tests mock CCC; no live LLM in `npm test`.
- Companion CCC holds Master CV, smoke, inbox worker, and overall product STRATEGY.
- Prefer `docs/plans/README.md` as the CE milestone north-star.
- `docs/solutions/` — documented solutions to past problems (bugs, best practices, workflow patterns), organized by category with YAML frontmatter (`module`, `tags`, `problem_type`). Relevant when implementing or debugging in documented areas.
- `CONCEPTS.md` — shared domain vocabulary (entities, named processes, status concepts) — relevant when orienting to the codebase or discussing domain concepts.
