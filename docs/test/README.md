# Test

Test strategy notes for this frontend.

- `npm test` — unit tests with mocked CCC; no live LLM.
- `npm run lint` / `npm run build` — static checks.
- Local proof of tailor UI: `npm run dev` with CCC reachable at `CCC_API_URL`.
